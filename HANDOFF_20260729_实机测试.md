# 实机无夹爪测试交接记录

更新时间：2026-07-29，Asia/Shanghai

## 当前目标

在 PX4 真机上完成无夹爪 UDP 流程：模拟 GCS 接收并 ACK 全部 `ok_*`，发送
`run_plan1`、连续 `car_status`、`match_car_ok`、`b_ok`；无人机相对 Init 起飞 1.5 m，
稳定 3 s，执行右前方 60 度 1 m（1 s）与正前方 0.5 m 的模拟目标指令，随后返回 Init XY，
下降、Disarm、切回 MANUAL。现场人员持有刹车。

## 已确认的协议决定

- 每个离散业务 UDP 命令前等待 4--9 s；当前实机模拟器取 4 s。
- `ack` 是收到 `ok_*` 后的即时传输确认，不采用 4 s 业务命令等待。
- `car_status` 是 10 Hz 连续状态，不采用离散命令等待。
- GCS 可自行按地图/业务条件决定 `b_ok` 时机；不得在 UAV 逻辑中写死 `(3.5, 1.5)`。
- UAV 只在 `awaiting_b_ok` 接受 `b_ok`，并返回自身锁存的 Init 相对 XY 原点 `(0, 0)`。
- `position_z_m`、`z_valid` 和 Z 数据源不是 `b_ok` 门禁。

## 已完成的实现

- `mission.takeoff_height_m` 为 1.5 m，`mission.height_stable_seconds` 为 3 s；状态机仅在
  实测高度和 XY 均稳定后发送 `ok_height`。
- 已有循环 UDP GTest：
  `mavros_xyz_position_offboard/test/test_simulated_ground_station.cpp`。
- 已新增实机模拟 GCS：
  `mavros_xyz_position_offboard/test/simulated_ground_station_udp.py`。
  它绑定本机 UDP 端口，立即 ACK `ok_*`，按 4 s 等待发送离散命令，并依次发送
  `distance=1.10, angle=-60`、`distance=0.60, angle=0`、停靠 `distance=0.10`。
- `launch/flight_stack.launch.py` 已支持：
  `udp_*` 端点参数、默认关闭的 `enable_bounded_flight`、以及必须非空的
  `flight_evidence_label`。默认实例仍不发送设定点、不请求 OFFBOARD/ARM。
- `GCS_UAV_JSON通信协议.md`、`联合任务系统架构与通信协议.md` 和包 README 已同步说明
  `b_ok` 为 GCS 自行决策，UAV 返回 Init XY。

## 本次实机尝试与安全状态

1. 断电后重新启动了仅监控栈，已确认：MAVROS 已连接、未解锁、MANUAL、已着陆，电池约
   16.3 V，`/lcp/status=2`，LCP 地图锁定、位姿有效，完整预检进入 `waiting_run_plan1`。
2. 受控实例和本机 GCS 曾成功完成 UDP `ok_wait`/ACK，进入 OFFBOARD、ARM 与 `climb`。
3. 爬升未成功，操作者已刹车。随后停止了模拟 GCS 与受控启动栈；后续只重启了
   `udp_enabled:=false` 的监控栈。最近审计记录显示 `armed=false`、`landed_state=1`。
4. 本次实机流程未完成，不能将其报告为通过，也不得在未明确再次授权前自动重试起飞。

## 已定位的根因与最终修复

导航器和 `/mavros/local_position/pose` 使用 ROS ENU。已确认安装的 MAVROS 2.14
`SetpointRawPlugin::local_cb()` 对 `FRAME_LOCAL_NED` 输入调用 `StaticTF::ENU_TO_NED`。
因此应用侧再手工执行 `(x,y,z)_NED=(y,x,-z)_ENU` 和 yaw 映射会发生**重复转换**，使
实际 PX4 指令错误；这可以解释上一轮未能正常爬升。

最终契约是：应用将 ROS ENU 的 `x/y/z/yaw` 原值写入 MAVROS `PositionTarget`，由 MAVROS
负责转换为 PX4 NED。不能只将原先的正负号对调，因为 X/Y 交换、Z 取反和 yaw 变换会被
MAVROS 再执行一次，仍然得到错误的最终 NED 指令。

已对以下文件恢复 ENU 直通并补充断言，尚未完成修复后的构建/测试：

- `mavros_xyz_position_offboard/src/offboard/offboard.cpp`
- `mavros_xyz_position_offboard/include/mavros_xyz_position_offboard/offboard/offboard.hpp`
- `mavros_xyz_position_offboard/test/test_lcp_navigation.cpp`
- `mavros_xyz_position_offboard/README.md`

特别检查：`test_lcp_navigation` 的 Offboard 映射测试现在应断言 ENU `(1,2,3,yaw=0)`
发布给 MAVROS 时仍是 `(1,2,3,yaw=0)`。

## 本轮预解锁异常及安全修复

本轮 `run_plan1` 已被接收，状态机依次进入 `setpoint_warmup` 和
`offboard_request_pending`，飞控已短暂切入 OFFBOARD。随后预检在解锁前回退，旧逻辑仅将
导航器重置为等待状态，无法撤销先前已发出的 ARM 服务请求；审计记录到飞控曾短暂
`armed=true`。操作已停止模拟器、执行 MANUAL 请求，并确认最终 `MANUAL`、未解锁、已着陆。

在 `waiting_run_plan1`、预热、OFFBOARD 请求或 ARM 请求阶段丢失预检时，任务不再静默 reset，
而是复用既有 `manual_request_pending` 安全收尾路径，持续请求 Disarm 与 MANUAL，待飞控确认后
进入 `manual`。这避免新增独立状态与空中阶段判定耦合。审计 JSON 新增 `preflight_errors` 和
`flight_errors` 字段；回归测试覆盖“MANUAL 模式下预检丢失不得进入 landing”。

## 必须完成的下一步

1. 在新窗口先检查上述转换补丁是否位于工作树，运行：

   ```bash
   cd /home/pi/px4-test-tools
   source /opt/ros/jazzy/setup.bash
   source /home/pi/LSLIDARN10P/install/setup.bash
